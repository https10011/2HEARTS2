package com.twohearts.app.config

/**
 * AppInfo — application metadata.
 * Matches legacy AppInfo configuration.
 */
object AppInfo {
    const val APP_ID = "com.twohearts.app"
    const val APP_NAME = "TwoHearts"
    const val VERSION_NAME = "1.0.0"
    const val VERSION_CODE = 1
    const val MIN_SDK = 26
    const val TARGET_SDK = 35
}
